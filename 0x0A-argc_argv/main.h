#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

int *array(int c, int arr[]);
int _log(int n);
int checker(char *s);
void buffer(char *s);
int _putchar(char c);

#endif
